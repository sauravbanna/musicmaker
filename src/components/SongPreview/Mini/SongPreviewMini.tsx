import {ISongPreviewMiniProps} from "../SongPreviewInterface"

const SongPreviewMini = ({id, title, author, duration, image}: ISongPreviewMiniProps) => {
    return (
        <div> f </div>
    );
}

export default SongPreviewMini